110a111,114
> #if CONFIG_OV2640_SUPPORT
> 	    mp_task_heap += 512 * 1024;  /* keep some space for the image buffer */
> 	    mp_task_heap_size = 4 * 1024 * 1024 - 512 * 1024; 
> #else	  
111a116
> #endif
142a148,149
>     mp_obj_list_init(mp_sys_path, 0);
>     mp_obj_list_append(mp_sys_path, MP_OBJ_NEW_QSTR(MP_QSTR_));
143a151
>     mp_obj_list_init(mp_sys_argv, 0);
